# ─── CF Advisor Suite — Variabili d'ambiente ───────────────────────────────
# Copia questo file come .env.local per sviluppo locale
# Su Vercel: aggiungi queste variabili in Settings → Environment Variables

# ── URL dell'app (senza slash finale) ──────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://tua-app.vercel.app

# ── Anthropic (AI generazione contenuti) ───────────────────────────────────
# Ottieni su: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# ── LinkedIn OAuth ──────────────────────────────────────────────────────────
# 1. Vai su https://developer.linkedin.com/ → crea un'app
# 2. Aggiungi prodotti: "Share on LinkedIn" + "Sign In with LinkedIn using OpenID Connect"
# 3. In "Auth" → aggiungi Redirect URL: https://tua-app.vercel.app/api/auth/linkedin-callback
# 4. Scope necessari: openid, profile, w_member_social

NEXT_PUBLIC_LINKEDIN_CLIENT_ID=86xxx...        # visibile, usato nel frontend per il link OAuth
LINKEDIN_CLIENT_SECRET=xxx...                  # segreto, solo backend
LINKEDIN_ACCESS_TOKEN=                         # compilato automaticamente da /auth/setup
LINKEDIN_PERSON_ID=                            # compilato automaticamente da /auth/setup

# Indicatore visivo connessione (imposta 'true' dopo setup completato)
NEXT_PUBLIC_LINKEDIN_CONFIGURED=false

# ── Meta / Instagram ────────────────────────────────────────────────────────
# Requisiti: Account Instagram Business + Pagina Facebook collegata
# 1. Vai su https://developers.facebook.com/ → crea app tipo "Business"
# 2. Aggiungi prodotto "Instagram Graph API"
# 3. Ottieni Page Access Token da Graph API Explorer con permessi:
#    instagram_content_publish, pages_read_engagement, instagram_basic
# 4. Chiama GET /me/accounts per trovare l'instagram_business_account id

INSTAGRAM_ACCESS_TOKEN=                        # Page Access Token da Graph API Explorer
INSTAGRAM_ACCOUNT_ID=                          # ID account Instagram Business (17841xxx)
INSTAGRAM_DEFAULT_IMAGE_URL=                   # URL immagine default per post senza immagine

# Indicatore visivo connessione
NEXT_PUBLIC_INSTAGRAM_CONFIGURED=false
